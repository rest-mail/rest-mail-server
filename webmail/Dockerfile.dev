# Development Dockerfile for the React webmail frontend with hot reload
FROM node:18-alpine

WORKDIR /app

# Copy package files and install dependencies
COPY package.json package-lock.json ./
RUN npm ci

# Source code is mounted as a volume at runtime
EXPOSE 3000

CMD ["npm", "run", "dev"]
