root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/imap-gateway ./cmd/imap-gateway"
  bin = "tmp/imap-gateway"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "webmail", "website", "node_modules"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
