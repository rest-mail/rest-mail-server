root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/pop3-gateway ./cmd/pop3-gateway"
  bin = "tmp/pop3-gateway"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "webmail", "website", "node_modules"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
